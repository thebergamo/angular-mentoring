import { CourseFreshnessDirective } from './course-freshness.directive';

describe('CourseFreshnessDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseFreshnessDirective();
    expect(directive).toBeTruthy();
  });
});
